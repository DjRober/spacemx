# node_modules del host: se reinstala dentro de la imagen con npm ci
# (el del host viene compilado para Windows y romperia el contenedor).
node_modules
npm-debug.log*

# Build de produccion: el contenedor corre Vite en modo dev.
dist
dist-ssr

.env
.env.*
!.env.example

Dockerfile
.dockerignore
README.md
